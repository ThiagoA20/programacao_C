#include <SDL2/SDL.h>
#include <iostream>

#define fps 60

using namespace std;

int main(int argc, char *argv[]) {

    // 1º init sdl
    // ------------------------------------------------------ Init SDL functions

    // 2º create window
    // ------------------------------------------------------ Set SDL window variable as window
    // ------------------------------------------------------ Parameters: title, x, y, w, h, flags...
    // ------------------------------------------------------ Check if the window was created

    // 3º Event listener
    // ------------------------------------------------------ Start the main loop
    // ------------------------------------------------------ Set starting tick for this while execution
    // ------------------------------------------------------ Detect events
    // ------------------------------------------------------ Quit when click on X
    // ------------------------------------------------------ Print Key pressed
    // ------------------------------------------------------ Limit the frame per second to 60

    // 4º Destroy window / terminate sdl process
    // ------------------------------------------------------ Delete window
    // ------------------------------------------------------ Terminate SDL functions

    return 0;
}